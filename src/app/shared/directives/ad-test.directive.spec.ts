import { AdTestDirective } from './ad-test.directive';

describe('AdTestDirective', () => {
  it('should create an instance', () => {
    const directive = new AdTestDirective();
    expect(directive).toBeTruthy();
  });
});
