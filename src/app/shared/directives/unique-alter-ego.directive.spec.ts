import { UniqueAlterEgoDirective } from './unique-alter-ego.directive';

describe('UniqueAlterEgoDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueAlterEgoDirective();
    expect(directive).toBeTruthy();
  });
});
